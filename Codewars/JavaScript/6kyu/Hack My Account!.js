function hack(username, password){
  return [Object.prototype.myUsername, Object.prototype.encrypt(Object.prototype.myPassword)]
}

--------------------------------------------------------------------------------------------

function hack() {
  return [hack.myUsername, hack.encrypt(hack.myPassword)];
}
